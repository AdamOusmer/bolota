# test/site

Vanilla Vite + TS page exercising `blobatar` as a real dependency (`file:../..`): a live determinism checker (byte-compares `blobatar()` calls per keystroke), all 13 core expressions, and all 15 bloub engine states.

Run: `bun install && bun run dev`. Build check: `bunx vite build`.
