# test/site

Vanilla Vite + TS page exercising `bolota` as a real dependency (`file:../..`): a live determinism checker (byte-compares `bolota()` calls per keystroke), all 13 core expressions, and all 13 bloub engine states.

Run: `bun install && bun run dev`. Build check: `bunx vite build`.
